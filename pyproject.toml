[project]
name = "pyrtlsweeper"
version = "0.1.0"
readme = "README.md"
authors = [{ name = "Edwin Chang" }]
requires-python = ">=3.13"
dependencies = [
    "hatchling>=1.27.0",
    "pyrtl>=0.11.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
